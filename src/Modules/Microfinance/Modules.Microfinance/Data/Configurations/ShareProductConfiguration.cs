using FSH.Modules.Microfinance.Domain;
using Microsoft.EntityFrameworkCore.Metadata.Builders;

namespace FSH.Modules.Microfinance.Data.Configurations;

public class ShareProductConfiguration : IEntityTypeConfiguration<ShareProduct>
{
    public void Configure(EntityTypeBuilder<ShareProduct> builder)
    {
        builder.ToTable("ShareProducts", "microfinance");
        builder.HasKey(x => x.Id);
        builder.Property(x => x.Name).IsRequired().HasMaxLength(256);
        builder.HasIndex(x => x.TenantId);
    }
}
